NotebookList 単語帳一覧
NotebookDetail　ノート詳細
WordCard 単語カード

App.jsx　★全部のまとめ役
　　App.jsxのstate(状態)
    notebooks(ノートの一覧)
    selectedNotebook(どのノートを開いているか)

    App.jsxの機能
    単語帳追加
    単語帳の削除
    単語追加
    単語削除
    画面の切り替え

npm run dev


git status          # 変更内容を確認
git add .
git commit -m "変更内容のコメント"
git push


https://knowbuddy.vercel.app

ブランチの確認
git branch　

※mainになっていたらOK
もし違うなら↓のコマンドを実行
git push origin main


*UPDATREコマンド　（すっごく大事）
git add .
git commit -m "update"
git push

<<<<<<< HEAD
ここから↓編集した後の反映↓

もしブラウザが古い場合は Ctrl+Shift+R で強制リロード
git status

git add .

git commit -m "Describe your change"

git push origin main


🔹 4️⃣ Vercel でデプロイ確認

GitHub に push すると Vercel が自動でビルド

Vercel のダッシュボードで Deployments を開く

最新デプロイが Ready になると反映完了

本番 URL で確認できます（例: https://know-buddy.vercel.app）

💡 ポイント：

デプロイには数秒〜1分程度かかる

自動で本番環境に反映されるので手動操作は不要

🔹 5️⃣ 編集 → 保存 → 反映の流れまとめ

VS Code で編集

保存（Ctrl+S）

git add . && git commit -m "..."

git push origin main

Vercel が自動で更新

ブラウザで確認
=======
git fetch origin
git reset --hard origin/main

🔵 いちばん確実（迷ったらこれ）

学校のPCで：

KnowBuddyフォルダ削除

もう一度クローン

git clone https://github.com/miraikami111/knowbuddy.git
cd knowbuddy
npm install
npm run dev

🎯 学校で作業するときの黄金ルール
🔹 作業前
git pull

🔹 作業後
git add .
git commit -m "update"
git push

🌱 さらに安心するコツ

学校で作業するとき：

git branch


で必ず * main になっているか確認。
>>>>>>> e5525cb (updated215)
